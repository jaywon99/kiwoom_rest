# TASK-001: 토큰 만료 시 자동 갱신 로직 추가

## 상태
- 상태: 완료
- commit_approval: status: approved

## 개요
- 만료된 토큰으로 키움 API 호출 시 에러(8005) 발생
- 프록시 서버에서 이를 감지하고 자동으로 토큰을 재발급받아 요청을 재시도하도록 수정

## 작업 내역
- `main.py`의 `proxy_request` 수정
- 키움 API 에러 응답(`return_code=3` 또는 `8005:Token이 유효하지 않습니다`) 감지 로직 추가
- 에러 감지 시 캐시된 토큰 삭제 및 재발급 후 원래 요청 재시도
